{
  "name": "Project-Title",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Tests require Hardhat; install or restore Hardhat to run tests\" && exit 0",
    "compile": "echo \"Compile step removed (Hardhat was removed)\" && exit 0",
    "deploy": "echo \"Deploy script requires Hardhat; restore Hardhat to use this\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.3",
    "chai": "^4.3.7"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.3"
  }
} 